# Heart_Disease_Prediction_Using_Supervised_Machine_Learning_Algorithms

The project is done using Flask Framework

Required Python Libraries
 - pip install flask
 - pip install pymysql
 - pip install sklearn
 - pip install numpy
 - pip install pandas
 - pip install joblib
 - pip install matplotlib


Reqired to create Database 
 - create database name = "healthcare"
 - create table = "userinfo"
 - in userinfo add two attributes = user, password
 - Done

In database.py the database having password then add possword in line No 5 and 24.

Thank You !!!
